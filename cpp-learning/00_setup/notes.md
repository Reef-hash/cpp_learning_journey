# Setup Notes

- Confirm your compiler supports C++17 or newer.
- Build with debug flags during learning (`-g`) to inspect memory in `gdb`.
- Prefer warnings enabled: `-Wall -Wextra -Wpedantic`.
- Keep each lesson small and focused on one concept.
